const CustomerLogin = (email,otp) =>{
    return new Promise(async (resolve, reject) => {        
        // resolve({"error":false})
        reject("validation failed")
    })
}

module.exports= {CustomerLogin}